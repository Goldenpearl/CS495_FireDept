Digital Ocean Console 
 **download putty here: http://www.chiark.greenend.org.uk/~sgtatham/putty/download.html
 **

MySQL: Before moving into a production environment:
 **Remove anonymoyous users
 **Remove test database
 **Disallow remote login for root


GO:
sudo apt-get update
sudo apt-get install mysql-server

sudo mysql_install_db
sudo mysql_secure_installation
sudo nano /etc/mysql/my.cnf

//find the section labeled [mysqlid] and rename bind-address to server ip
//bind-address        = your_database_IP

sudo service mysql restart
mysql -u root -p
CREATE DATABASE wordpress;
CREATE USER 'wordpressuser'@'localhost' IDENTIFIED BY 'wordpie';
GRANT ALL PRIVILEGES ON wordpress.* TO 'wordpressuser'@'localhost';
CREATE USER 'wordpressuser'@'162.243.8.101' IDENTIFIED BY 'wordpie';
GRANT SELECT,DELETE,INSERT,UPDATE ON wordpress.* TO 'wordpressuser'@'162.243.8.101';
GRANT ALL PRIVILEGES ON wordpress.* TO 'wordpressuser'@'162.243.8.101';
FLUSH PRIVILEGES;
exit

mysql -u wordpressuser -p
//type in password to test
exit

sudo apt-get update
sudo apt-get install mysql-client

mysql -u wordpressuser -h 162.243.8.101 -p
//this tests remote connection

sudo apt-get install nginx php5-fpm php5-mysql
sudo nano /etc/php5/fpm/php.ini
//Search for the cgi.fix_pathinfo parameter. 
//It will likely be commented out with the ";" character and set to "1". 
//We need to reverse both of these conditions by uncommenting the line 
//and setting it to "0":
//cgi.fix_pathinfo=0
//save file
sudo nano /etc/php5/fpm/pool.d/www.conf
//Look for the listen directive, which should be set to 127.0.0.1:9000
//Rather than using a port, we're going to set this to a unix domain socket:
//listen = /var/run/php5-fpm.sock
//save
sudo service php5-fpm restart
sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/yorktownfire.xyz
sudo nano /etc/nginx/sites-available/yorktownfire.xyz


//Inside, we'll modify our server block (the sections contained within the 
//server brackets). Begin by uncommenting the directive to 
//listen to port 80. We're also going to change the root directory 
//and make Nginx serve a PHP index file by default:





*** Could not make these lines work
sudo usermod -a -G www-data your_user
sudo chmod -R g+rw /var/www/example.com